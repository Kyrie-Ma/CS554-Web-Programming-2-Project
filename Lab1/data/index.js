const likesData = require('./likes');
const replyData = require('./reply');
const sweetsData = require('./sweets');
const userData = require('./user');

module.exports = {
  likes: likesData,
  reply: replyData,
  sweets: sweetsData,
  user: userData,
};